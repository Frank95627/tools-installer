extends layout

block content
  h1 Buscador Global
  form(action="/search" method="get")
    input(type="text" name="query" placeholder="Buscar notas o información..." required)
    input(type="submit" value="Buscar")
